{
  "name": "aks-configurator-test",
  "private": true,
  "type": "module",
  "description": "CI harness that loads aks-configurator.html in jsdom, invokes generateBicepParam + extracts BICEP_TEMPLATES, so az bicep build can validate the emitted package.",
  "scripts": {
    "generate": "node generate-package.mjs"
  },
  "dependencies": {
    "jsdom": "^24.1.0"
  }
}
